{
  "name": "contracts",
  "version": "1.0.0",
  "description": "Smart contracts for TokenizeArt NFT platform",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run deploy.js --network sepolia",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^16.0.0",
    "ethers": "^6.13.0"
  }
}
